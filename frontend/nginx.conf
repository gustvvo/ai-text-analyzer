server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: client-side routes (/analyze, /results/:id, /history)
    # must serve index.html, not 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
